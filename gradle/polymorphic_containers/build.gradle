apply plugin: 'cpp'

// global c++ compile flags
model {
  binaries {
    all {
      cppCompiler.args '-std=c++11'
      cppCompiler.args '-Werror=switch-enum'
    }
  }
}

model {
  components {
    hello(NativeExecutableSpec)
  }
}

model {
  components {
    hello.baseName = "helloExe" 
  }
}